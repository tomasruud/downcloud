{
  "name": "downcloud",
  "version": "2.0.0-alpha",
  "private": true,
  "scripts": {
    "lint": "standard --fix",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "npm run lint && react-scripts test --env=jsdom"
  },
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "standard": "^11.0.1"
  },
  "standard": {
    "ignore": "old/**"
  }
}
