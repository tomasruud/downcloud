language: node_js
node_js:
  - 14
notifications:
  email: false
script:
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  fqdn: downcloud.ruud.ninja
  on:
    branch: master
