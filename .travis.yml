language: node_js
node_js:
  - node
install:
  - npm install
script:
  - npm run build
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  local-dir: build
  fqdn: downcloud.ruud.ninja
  on:
    branch: master